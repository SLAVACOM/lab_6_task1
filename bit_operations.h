#pragma once
#include <cstdint>

// Прототип функции побитового сдвига
uint64_t bit_shift_odd_right(uint64_t number, int shift);

bool isValidFloat(const char* str);

bool isValidShift(const char* str);